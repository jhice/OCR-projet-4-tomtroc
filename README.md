# Projet Tom Troc

- Démarrer le serveur de dev PHP via `php -S localhost:8000 -t public` depuis la racine du projet.
- Lancez l'initialisation de la BDD via `mysql -u tomtroc -p tomtroc < sql/init_db.sql`.